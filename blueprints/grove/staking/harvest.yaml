protocol: "grove-staking"

inputs:
  staking_contract_address:
    type: "address"

actions:
  harvest:
    calls:
      - description: "Claim accrued reward tokens from StakingRewards"
        target: "${inputs.staking_contract_address}"
        selector: "getReward()"
        parameters: []
